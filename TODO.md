# Agor iOS — Bugs & Feature Requests

## Bugs

- [x] **HTTPS URLs get port 3030 appended, breaking reverse proxy setups** — In `AuthService.swift` line 61, `let port = components.port ?? 3030` always defaults to 3030 even for HTTPS URLs. When user enters `https://agor-rmi.duckdns.org/`, it becomes `https://agor-rmi.duckdns.org:3030` which fails because the daemon is behind a reverse proxy on port 443. Fix: only default to `:3030` when scheme is `http` (local dev). For `https`, omit the port entirely (443 is implied). The web UI works because `getDaemonUrl()` in `daemon.ts` uses `window.location.origin` which doesn't append a port for standard HTTPS.
- [x] **File browser fails with "Session expired. Please log in again."** — The iOS app used REST (URLSession) for the `/file` API, but the web UI uses Socket.IO where auth is resolved at the socket connection level. Fix: changed `FileBrowserViewModel` to use `SocketService.serviceFind`/`serviceGet` via Socket.IO instead of REST, matching the web UI's approach.
- [x] **"Archive & Reset" only archives, doesn't start a new session** — The reset button shows a confirm popup with "Archive and Reset", but after confirming it only archives the current session — no new session is created in the same worktree. Expected behavior: archive the old session, then create a new session on the same worktree (via `POST /sessions` with the same worktree_id and agentic_tool), and navigate to it in the sidebar/chat.
- [x] **Local notifications unreliable — only fire sometimes when app is open, never when backgrounded** — Two issues: (1) The `setupCrossSessionNotifications()` closure captures `[self]` (a struct), so `wasBackgrounded`, `selectedSessionId`, and `previousSessionStatuses` are frozen snapshots that never update. `previousSessionStatuses` is also mostly empty because sessions load lazily. (2) When backgrounded, the socket disconnects so no events arrive at all. Fix: move notification state into an `@Observable` class with reference semantics so closures read live values. For background: keep socket alive briefly via `BGTaskScheduler` or `beginBackgroundTask`, and/or add a fallback polling mechanism on app resume to check for missed status transitions.
- [x] **Sidebar caching causes flashing/flickering** — The session list flickers and repopulates on every refresh. The current cache implementation replaces the entire list, causing all items to disappear and reappear. Fix: use a dictionary/map keyed by ID as the source of truth. On refresh, diff against the existing map — only add new items, remove deleted ones, and update changed ones in-place. This prevents the list from flashing. The same pattern should apply to boards, worktrees, and sessions. Never replace the whole array — merge incrementally.
- [x] **Session context menu missing actions** — The context menu (long-press) on session rows in the sidebar only shows "Browse Files". It should also include "Clean/Reset" (archive current session and create a new one on the same worktree), "Archive", and "Add to Favorite" actions. These are essential session management operations that users expect to access directly from the sidebar without opening the session first.
- [x] **Archive & Reset creates session with wrong name** — When using "Archive & Reset", the new session gets a different (auto-generated) name instead of keeping the original session's name. The `CreateSessionBody` in `ChatViewModel.resetSession()` only passes `worktree_id`, `agentic_tool`, and `status` — it doesn't pass the original session's `name`. Fix: include the original session's name in the POST body so the replacement session has the same name.
- [x] **File browser shows "data couldn't be read because it is wrong format"** — The file browser fails to decode the response from the Socket.IO `find "file"` service call. The `FileListItem` model likely doesn't match the actual JSON structure returned by the backend. Need to check the debug log for the raw response data and compare it against the Swift Codable model.
- [x] **Tapping a notification doesn't navigate to the session** — When a local notification fires and the user taps it, nothing happens — the app opens but doesn't navigate to the session that completed. The `userInfo["sessionId"]` is set in `fireLocalNotification()` but there's no `UNUserNotificationCenterDelegate.didReceive` handler to read it and navigate. Fix: add `didReceive` to `NotificationDelegate` in `AgorApp.swift`, extract the sessionId from userInfo, and set it as the selected session.
- [x] **Session context menu under boards only shows "Browse Files"** — Long-pressing a session row inside a board's worktree section shows only "Browse Files", missing "Add to Favorites", "Clean & Reset", and "Archive". The code in `WorktreeSection` (SidebarView.swift ~line 397) has all items defined, but the worktree-level context menu (line 418) only has "Browse Files" and may be intercepting the gesture before the session context menu triggers. Need to verify that session rows inside `WorktreeSection` actually receive the long-press (not the parent `DisclosureGroup`/worktree row), and ensure all context menu items render.
- [x] **Session rows still flicker on update** — When a session is updated (e.g. status change), the entire row panel flickers instead of smoothly updating just the text/icons that changed. The `Session` model is a struct, so SwiftUI treats any property change as a full replacement. Fix: use fine-grained identity — ensure `ForEach` uses stable IDs and session rows only re-render the specific subviews (status badge, title text) that actually changed, not the whole row. Consider making session properties individually observable or using `Equatable` conformance to minimize diffing.
- [x] **Socket query parameter nesting breaks file browser** — `serviceFind` and `serviceGet` in `SocketService.swift` wrapped the query in `{"query": {...}}`, but FeathersJS transport-commons expects the flat query at `methodArgs[0]`. Backend received `{ query: { query: { worktree_id: "..." } } }` and couldn't find the parameter. Fix: send `query` directly instead of `["query": query]`.
- [x] **Error responses without `code` field treated as success** — `parseFeathersAck` only detected errors with a `code` key. Plain JavaScript `Error` objects serialize as `{"message": "..."}` without `code` (only FeathersJS error classes include it). These slipped through as success responses. Fix: added check for single-element responses with `message` key but no `code`.
- [x] **File browser `loadFiles()` called 6+ times simultaneously** — SwiftUI `.task` fires multiple times, and the `files.isEmpty` guard didn't prevent concurrent calls since all fired before the first completed. Fix: added `isLoading` guard in `loadFiles()`.
- [x] **Stuck thinking spinner after returning from background** — When backgrounded during `thinking:start`, socket disconnects and `thinking:end` is missed. `StreamingService.activeStreams` retains stale entries with `isThinking: true`. Fix: added `clearStreams(for:)` and call it during `refreshCurrentSession()` and when session patches to idle.
- [x] **Notifications only fire when app is open** — Socket disconnects when backgrounded, missing running→idle transitions. Fix: added `BGTaskScheduler` with `BGAppRefreshTask` polling session statuses every ~5 minutes in background. Uses `NotificationManager.checkMissedTransitions()` for missed transitions.
- [x] **Large empty space below messages after returning from background** — After the app wakes from sleep and refreshes the chat, there is a large blank area below the last message. The scroll position jumps past the content so only empty space is visible and messages are not shown. Likely caused by `scrollToBottomToken` triggering a scroll to the `"bottom"` anchor after `refreshCurrentSession()` reloads messages, but the `LazyVStack` hasn't finished laying out the new content yet, so the scroll target is at an incorrect offset. Fix: debounce or delay the scroll-to-bottom after reconnect, or only scroll if the user was already near the bottom. Also check if the `Color.clear.frame(height: 1).id("bottom")` anchor outside `LazyVStack` is contributing to incorrect layout measurements after content replacement.
- [x] **File browser loses files when session is running** — When a prompt is sent and the file browser is opened, all files disappear. The `FileBrowserView` creates a new `FileBrowserViewModel` each time the sheet opens (in `ChatView.swift`), so there's no cached state. When the session is running, the socket may be busy with streaming events. Fix: cache the file list either in a shared view model or by persisting `FileBrowserViewModel` across sheet presentations (use `@State` on the parent instead of creating inline). The file list for a worktree should survive sheet dismiss/reopen and only reload on explicit refresh or after a reasonable TTL.
- [x] **Messages appear in wrong order after returning from background** — Permission dialog messages and model responses appear at wrong positions in the chat after the app reconnects from background. Root cause: `_rebuildDisplayItemsNow()` in `ChatViewModel.swift` groups messages by `taskId` using `Dictionary(grouping: messages)`, which preserves array insertion order — not message `index` order. Socket-delivered messages (via `onMessageCreated`) are appended to the `messages` array end regardless of their `index` value. After reconnect, REST loads messages sorted by index, then new socket messages get appended. Within a task group, these late-arriving messages appear last even if their `index` places them earlier (e.g. a permission request at index 5 appears after a response at index 8). Fix: sort `taskMessages` by `index` before appending to display items: `taskMessages.sorted { ($0.index ?? 0) < ($1.index ?? 0) }`.

## Feature Requests

- [x] **Debug log viewer with "Send to session" action** — Add an in-app debug log that captures all significant events and errors (API calls, WebSocket events, auth flows, errors, state changes). The log should be viewable from a settings/debug screen. Critical feature: a "Send to Session" button that lets the user pick a session and post the log content as a message in that session's chat. This enables easy debugging by sharing the app's log with an AI agent session. Implementation notes: use a singleton `AppLogger` that stores timestamped log entries in memory (with a rolling cap, e.g. 500 entries). Add a "Debug Log" row in Settings. The send action should use `POST /sessions/:id/prompt` (or the messages API) to post the log text into the chosen session.
- [x] **Attachment context menu in prompt input bar** — Added paperclip button with three options: Attach Debug Log (exports log as .txt), Attach Photo (PhotosUI picker), Attach File (system file picker). Files uploaded via `POST /sessions/:id/upload` (multipart/form-data) to worktree's `.agor/uploads/`. After upload, `@filepath` reference inserted into prompt text for agent access.

## Open Feature Requests

- [x] **Spawn new session from worktree context menu** — Add "New Session" action to the worktree context menu in the sidebar. Shows a simple dialog with just a **Name** field. Model/agentic tool always defaults (`claude-code`, `idle`). On confirm: `POST /sessions` with `{ worktree_id, agentic_tool: "claude-code", status: "idle", title: name }`. Navigate to the new session in chat.

- [x] **Manage MCP servers on any session** — Add MCP server management to the chat toolbar (alongside file browser, archive, reset buttons). Tapping opens a sheet showing:
  - Current session's MCP servers (fetched from `GET /sessions/:id/mcp-servers`)
  - Available MCP servers to add (fetched from `GET /mcp-servers?scope=global&enabled=true`)
  - Toggle to enable/disable per server (`PATCH /sessions/:id/mcp-servers/:mcpId` with `{ enabled }`)
  - Add server button (`POST /sessions/:id/mcp-servers` with `{ mcpServerId }`)
  - Remove server (swipe to delete, `DELETE /sessions/:id/mcp-servers/:mcpId`)
  - **MCP Server model for iOS**: Add `MCPServer` Swift struct with fields: `mcpServerId`, `name`, `displayName`, `description`, `transport`, `enabled`. Fetch via `GET /mcp-servers` (REST, paginated response).
  - **Reference**: Backend API at `apps/agor-daemon/src/services/mcp-servers.ts`, session-MCP junction at `apps/agor-daemon/src/services/session-mcp-servers.ts`.

- [x] **File browser: download/open in other app** — Add context menu to each file row in `FileBrowserView` (long-press) with options to download or open in another iOS app. Save file content to a temp directory, then present iOS share sheet (`UIActivityViewController` / `ShareLink`) which allows saving to Files, AirDrop, opening in other apps, etc. For text files use `fileDetail.content`, for binary files may need a raw download endpoint.

- [x] **File browser: image zoom** — When viewing an image in `FileDetailView`, allow pinch-to-zoom and pan. Currently images are displayed at fixed size with no zoom capability. Use `MagnificationGesture` + `DragGesture` on the image, or wrap in a `ScrollView` with `zoomScale`. Double-tap to toggle between fit-to-screen and actual size.

- [x] **Chat: file path links + inline image previews** — When a message contains a file path (e.g. `@path/to/file.swift`, `spreads/spread-p06-p07.png`, or tool results like "Saved spreads/file.png"), render the path as a tappable link that opens the file browser navigated directly to that file. Additionally, if the referenced file is an image (png/jpg/gif/webp) and under 0.5MB, automatically fetch and display an inline image preview below the file link in the chat bubble. This applies to all messages — assistant text mentioning file paths, tool result outputs referencing saved files, and `@filepath` mentions.
  - **Path detection**: Scan message text for patterns like `path/to/file.ext`, `@filepath`, tool results with "Saved X", "Created X", "Wrote X" followed by a path. Use regex to detect common file extensions.
  - **Inline images**: For detected image paths, call `serviceGet("file", path, query: ["worktree_id": ...])` to fetch content. If file size < 0.5MB, decode and show `AsyncImage` or `Image(uiImage:)` inline in the message bubble, tappable to open full file detail.
  - **File links**: Non-image paths render as styled tappable text (blue, underlined). On tap: open `FileBrowserView` sheet → navigate to directory → show file detail.

- [x] **Chat: session hash mentions as tappable links** — When a message contains a session hash/ID (e.g. `f99e71f0`, `session 03b62447`, or a full UUID), render it as a tappable link that navigates to that session's chat view. Also detect markdown-style session links or `@session:hash` mentions. On tap: look up the session by short ID or full UUID (check `sessions` list or call `GET /sessions?id[$like]=hash%`), then navigate to that session's chat. If the session belongs to a different worktree, switch worktree context as well. This enables cross-session navigation directly from chat messages where agents reference other sessions.
  - **Detection**: Scan for 8-character hex strings that match known session short IDs, full UUIDs, or explicit `session:` prefixes. First check against the locally loaded sessions list. If no match found locally, fall back to an API call (`GET /sessions?id[$like]=hash%`) to resolve sessions that aren't cached (e.g. newly created sessions from other agents/devices). Cache resolved session lookups to avoid repeated API calls for the same hash.
  - **UI**: Render as styled tappable text (distinct color, e.g. purple/teal with session icon). On tap: push or present the target session's `ChatView`.

- [x] **Multi-server support: connect to different Agor instances** — Allow the user to configure and switch between multiple Agor server URLs (e.g. home server, work server, VPS). Implementation:
  - **Server profiles**: A list of named server entries stored in `UserDefaults` / local storage. Each entry has: name (e.g. "Home", "Work VPS"), URL (e.g. `http://192.168.1.50:3030`, `https://agor.example.com`), and optionally saved auth credentials/token.
  - **Settings screen**: Add a "Servers" section in Settings to manage server profiles — add, edit, delete, reorder. Each entry shows name, URL, and connection status indicator (green/red dot).
  - **Server switcher**: Add a quick-switch control in the sidebar header or top navigation bar showing the current server name. Tapping opens a picker/menu to switch to another configured server. Switching triggers: disconnect current socket, update `AgorClient` base URL, clear cached sessions/worktrees/repos, reconnect socket, re-authenticate, and reload all data.
  - **Per-server state**: Each server has its own auth token. On switch, restore the saved token for that server. If no token exists, show the login/auth flow.
  - **Default server**: One server is marked as default (used on app launch). First-time setup uses the current hardcoded URL as the initial server entry.
  - **Visual indicator**: Show current server name in sidebar header so it's always clear which Agor instance the app is connected to.

## Future Improvements

- [ ] Push notifications via APNs for real-time delivery without polling
- [ ] Background URL session for uploads that survive app suspension
- [ ] Attachment preview chips showing uploaded files before sending prompt
- [ ] Multiple file selection in file picker
